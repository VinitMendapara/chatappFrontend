const server_url = "http://localhost:3000"
const constants = {
    socket: server_url,
    login: `${server_url}/login`,
    register: `${server_url}/register`,
    users: `${server_url}/list`,
    chat: `${server_url}/chat`,
}

export default constants